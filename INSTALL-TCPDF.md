# 🚀 Быстрая установка TCPDF

## Для локальной разработки (Windows)

Если у вас установлен PHP локально, запустите:

```powershell
cd api
php install-tcpdf.php
```

## Для сервера (Synology NAS)

### Способ 1: Через SSH (рекомендуется)

1. **Подключитесь к Synology через SSH:**
   ```bash
   ssh admin@your-synology-ip
   ```

2. **Перейдите в директорию проекта:**
   ```bash
   cd /var/www/html/bso-portal/api
   ```

3. **Запустите установку:**
   ```bash
   php install-tcpdf.php
   ```

4. **Проверьте установку:**
   ```bash
   ls -la vendor/tecnickcom/tcpdf/tcpdf.php
   # или
   ls -la tcpdf/tcpdf.php
   ```

### Способ 2: Через веб-интерфейс Synology

1. Откройте **File Station** в Synology
2. Перейдите в `/web/bso-portal/api/`
3. Загрузите файл `install-tcpdf.php`
4. Откройте в браузере: `http://your-synology-ip/bso-portal/api/install-tcpdf.php`
5. Скрипт автоматически установит TCPDF

### Способ 3: Ручная установка через File Station

1. **Скачайте TCPDF:**
   - Перейдите: https://github.com/tecnickcom/TCPDF/releases/tag/6.6.5
   - Скачайте `TCPDF-6.6.5.tar.gz`

2. **Загрузите на Synology:**
   - Откройте File Station
   - Перейдите в `/web/bso-portal/api/`
   - Загрузите архив

3. **Распакуйте:**
   - Правый клик на архиве → Извлечь
   - Переименуйте `TCPDF-6.6.5` в `tcpdf`

4. **Проверьте права доступа:**
   - Правый клик на папке `tcpdf` → Свойства → Права
   - Убедитесь, что веб-сервер имеет права на чтение

## Проверка работы

После установки:

1. Откройте портал: `http://your-synology-ip/bso-portal/`
2. Перейдите в раздел **Помощь ИТ**
3. Заполните форму заявки
4. Нажмите **"Сохранить заявку"**
5. Должен скачаться PDF файл

Если PDF не генерируется, проверьте:
- Логи PHP в Synology
- Права доступа к папке `tcpdf` или `vendor`
- Версию PHP (требуется 7.0+)

## Структура после установки

```
api/
├── vendor/
│   └── tecnickcom/
│       └── tcpdf/
│           └── tcpdf.php  ✅ TCPDF здесь
├── helpdesk-pdf.php
└── install-tcpdf.php
```

Или:

```
api/
├── tcpdf/
│   └── tcpdf.php  ✅ TCPDF здесь
├── helpdesk-pdf.php
└── install-tcpdf.php
```

## Нужна помощь?

См. подробную инструкцию: `docs/tcpdf-installation-guide.md`
