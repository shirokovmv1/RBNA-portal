# Автоматическое развертывание на VPS

## 🚀 Полностью автоматический процесс

Этот скрипт автоматизирует **все** шаги развертывания на VPS сервере.

---

## 📋 Быстрый старт

### Шаг 1: Подготовка сервера (от root)

```bash
# На вашем локальном компьютере загрузите скрипты на сервер
scp deploy_full.sh deploy_application.sh root@your-server-ip:/root/

# Подключитесь к серверу
ssh root@your-server-ip

# Запустите скрипт подготовки
chmod +x deploy_full.sh
./deploy_full.sh yourdomain.com
```

**Что делает скрипт:**
- ✅ Обновляет систему
- ✅ Устанавливает все зависимости (Python, PostgreSQL, Nginx, Node.js)
- ✅ Создает пользователя rbna
- ✅ Настраивает PostgreSQL и создает БД
- ✅ Настраивает файрвол
- ✅ Создает структуру директорий

### Шаг 2: Загрузка проекта

**Вариант A: Через SCP (с локального компьютера)**

```bash
# Загрузите проект на сервер
scp -r backend/ frontend/ rbna@your-server-ip:/home/rbna/rbna-portal/
```

**Вариант B: Через Git (на сервере)**

```bash
# Подключитесь к серверу как пользователь rbna
ssh rbna@your-server-ip

# Клонируйте проект
cd /home/rbna/rbna-portal
git clone https://github.com/your-username/RBNA-portal.git .
```

### Шаг 3: Развертывание приложения (от пользователя rbna)

```bash
# Подключитесь как пользователь rbna
ssh rbna@your-server-ip

# Перейдите в директорию проекта
cd /home/rbna/rbna-portal

# Скопируйте скрипт развертывания
sudo cp /root/deploy_application.sh .
chmod +x deploy_application.sh

# Запустите автоматическое развертывание
./deploy_application.sh yourdomain.com
```

**Что делает скрипт:**
- ✅ Настраивает виртуальное окружение Python
- ✅ Устанавливает все зависимости backend
- ✅ Создает .env файл с правильными настройками
- ✅ Генерирует SECRET_KEY
- ✅ Настраивает PostgreSQL в settings.py
- ✅ Применяет миграции
- ✅ Загружает демо-данные (опционально)
- ✅ Создает суперпользователя Django
- ✅ Собирает статические файлы
- ✅ Настраивает Gunicorn
- ✅ Создает и запускает systemd сервис
- ✅ Собирает frontend
- ✅ Настраивает Nginx
- ✅ Устанавливает SSL сертификат (если возможно)

---

## 🎯 Полностью автоматический вариант (один скрипт)

Если проект уже в Git репозитории, можно объединить все в один скрипт:

```bash
# Создайте файл deploy_all.sh на сервере
cat > deploy_all.sh << 'EOF'
#!/bin/bash
set -e

DOMAIN=$1
GIT_REPO=$2

if [ -z "$DOMAIN" ] || [ -z "$GIT_REPO" ]; then
    echo "Использование: ./deploy_all.sh yourdomain.com https://github.com/user/repo.git"
    exit 1
fi

# Запуск подготовки сервера
./deploy_full.sh $DOMAIN

# Клонирование проекта
su - rbna << RBNA_EOF
cd /home/rbna/rbna-portal
git clone $GIT_REPO .
cd /root
sudo cp deploy_application.sh /home/rbna/rbna-portal/
chmod +x /home/rbna/rbna-portal/deploy_application.sh
/home/rbna/rbna-portal/deploy_application.sh $DOMAIN
RBNA_EOF

echo "✅ Развертывание завершено!"
EOF

chmod +x deploy_all.sh
./deploy_all.sh yourdomain.com https://github.com/your-username/RBNA-portal.git
```

---

## 📝 Что нужно подготовить

### Перед запуском скриптов:

1. **Доменное имя** настроено и указывает на IP вашего сервера
   ```bash
   # Проверка
   ping yourdomain.com
   ```

2. **SSH доступ** к серверу работает
   ```bash
   ssh root@your-server-ip
   ```

3. **Проект готов** к развертыванию
   - Backend код готов
   - Frontend код готов
   - requirements.txt актуален

---

## 🔄 Процесс в деталях

### Этап 1: Подготовка сервера (5-10 минут)

```bash
./deploy_full.sh yourdomain.com
```

**Результат:**
- Сервер готов к развертыванию
- Все зависимости установлены
- База данных создана
- Пароли сохранены в credentials.txt

### Этап 2: Загрузка проекта (зависит от скорости интернета)

```bash
# Загрузите проект
scp -r backend/ frontend/ rbna@your-server-ip:/home/rbna/rbna-portal/
```

### Этап 3: Развертывание (10-15 минут)

```bash
./deploy_application.sh yourdomain.com
```

**Результат:**
- Backend развернут и работает
- Frontend собран и развернут
- Nginx настроен
- SSL сертификат установлен (если возможно)
- Приложение доступно по HTTPS

---

## ✅ Проверка после развертывания

```bash
# Проверка всех сервисов
sudo systemctl status rbna-portal nginx postgresql

# Проверка работы API
curl https://yourdomain.com/api/users/

# Проверка логов
sudo journalctl -u rbna-portal -n 50
```

---

## 🆘 Решение проблем

### Проблема: Скрипт останавливается с ошибкой

```bash
# Проверьте логи
sudo journalctl -u rbna-portal -f

# Проверьте вручную
cd /home/rbna/rbna-portal/backend
source venv/bin/activate
python manage.py check
```

### Проблема: SSL не установился автоматически

```bash
# Установите вручную
sudo apt-get install certbot python3-certbot-nginx
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com
```

### Проблема: Проект не загрузился

```bash
# Проверьте права доступа
ls -la /home/rbna/rbna-portal/

# Проверьте наличие файлов
ls -la /home/rbna/rbna-portal/backend/
ls -la /home/rbna/rbna-portal/frontend/
```

---

## 📊 Время развертывания

- **Подготовка сервера**: 5-10 минут
- **Загрузка проекта**: зависит от размера и скорости
- **Развертывание**: 10-15 минут

**Итого: ~20-30 минут** для полностью автоматического развертывания

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет:
- ✅ Доступно по HTTPS
- ✅ Работать стабильно
- ✅ Автоматически запускаться при перезагрузке сервера
- ✅ Иметь настроенные логи
- ✅ Быть защищено файрволом

---

## 📚 Дополнительная информация

- Детальные инструкции: `VPS_DEPLOYMENT.md`
- Быстрые команды: `VPS_DEPLOYMENT_QUICK.md`
- Чеклист: `VPS_CHECKLIST.md`
