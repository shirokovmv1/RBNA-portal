# 🚀 Выполнение развертывания на сервер 151.245.137.147

## ✅ Готово к развертыванию!

Я подготовил специальный скрипт для вашего сервера. Выполните команды ниже.

---

## 📋 Команды для выполнения

### Шаг 1: Загрузите скрипт на сервер

**На вашем локальном компьютере (Windows PowerShell):**

```powershell
# Перейдите в директорию проекта
cd "C:\Users\shiro\OneDrive\Personal project\BSO\portal\bso-portal"

# Загрузите скрипт на сервер
scp deploy_to_151.245.137.147.sh root@151.245.137.147:/root/
```

**Или используйте WinSCP/FileZilla для загрузки файла.**

### Шаг 2: Подключитесь к серверу

```bash
ssh root@151.245.137.147
# Пароль: u14YDBo+u4
```

### Шаг 3: Запустите развертывание

**На сервере выполните:**

```bash
# Сделайте скрипт исполняемым
chmod +x /root/deploy_to_151.245.137.147.sh

# Запустите автоматическое развертывание
/root/deploy_to_151.245.137.147.sh
```

**Скрипт выполнит всё автоматически:**
- ✅ Установит все зависимости
- ✅ Настроит PostgreSQL
- ✅ Развернет backend
- ✅ Соберет frontend
- ✅ Настроит Nginx
- ✅ Запустит все сервисы

**Время выполнения: ~20-30 минут**

---

## 🔄 Альтернативный способ (если скрипт не загрузился)

### Выполните команды вручную на сервере:

```bash
# 1. Обновление системы
apt-get update && apt-get upgrade -y

# 2. Установка зависимостей
apt-get install -y python3 python3-pip python3-venv python3-dev \
    postgresql postgresql-contrib nginx git build-essential \
    libssl-dev libffi-dev ufw curl wget

# 3. Установка Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs

# 4. Клонирование проекта
cd /home
git clone https://github.com/shirokovmv1/RBNA-portal.git rbna-portal
cd rbna-portal

# Затем следуйте инструкциям из VPS_DEPLOYMENT.md
```

---

## ✅ После развертывания

### 1. Создайте суперпользователя

```bash
su - rbna
cd /home/rbna/rbna-portal/backend
source venv/bin/activate
python manage.py createsuperuser
```

### 2. Проверьте работу

Откройте в браузере:
- **http://151.245.137.147** - Frontend
- **http://151.245.137.147/api/users/** - API
- **http://151.245.137.147/admin/** - Админка Django

---

## 🔍 Проверка статуса

```bash
# Статус сервисов
systemctl status rbna-portal
systemctl status nginx
systemctl status postgresql

# Логи
journalctl -u rbna-portal -f
```

---

**Выполните команды из Шага 1-3 для автоматического развертывания! 🚀**
