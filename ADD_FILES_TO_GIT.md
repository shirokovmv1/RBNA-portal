# 🔧 Добавление файлов backend/ и frontend/ в Git

## Проблема

Файлы `backend/manage.py`, `backend/requirements.txt`, `frontend/package.json` и другие файлы из директорий `backend/` и `frontend/` не отслеживаются в Git репозитории. Поэтому после клонирования на сервере они отсутствуют.

## ✅ Решение

### Шаг 1: Удалить блокировку Git (если есть)

```bash
cd "c:\Users\shiro\OneDrive\Personal project\BSO\portal\bso-portal"
Remove-Item -Force .git/index.lock -ErrorAction SilentlyContinue
```

### Шаг 2: Добавить файлы в Git

```bash
# Добавить все файлы из backend/ и frontend/
git add backend/ frontend/

# Проверить статус
git status
```

### Шаг 3: Закоммитить изменения

```bash
git commit -m "Add backend and frontend files to repository"
```

### Шаг 4: Отправить в GitHub

```bash
git push origin main
```

---

## Альтернативное решение: Использовать скрипт, который создает файлы

Если не хотите коммитить файлы сейчас, можно использовать скрипт развертывания, который создаст необходимые файлы на сервере. Но это не рекомендуется для production.

---

## Проверка после коммита

После выполнения шагов выше, проверьте:

```bash
git ls-files backend/manage.py backend/requirements.txt frontend/package.json
```

Должны увидеть пути к файлам. Если пусто - файлы все еще не добавлены.
