# 🔧 Исправление ошибки развертывания

## Проблема

Скрипт `deploy_to_151.245.137.147.sh` не найден в репозитории, потому что он был создан локально и еще не закоммичен в Git.

## ✅ Решение

### Вариант 1: Создать скрипт прямо на сервере (рекомендуется)

**На сервере выполните эту команду:**

```bash
# Создать скрипт развертывания прямо на сервере
cat > /root/deploy_to_151.245.137.147.sh << 'SCRIPT_END'
# [Весь код скрипта будет здесь]
SCRIPT_END
```

**Или используйте готовую команду из файла `CREATE_DEPLOY_SCRIPT_ON_SERVER.sh`**

### Вариант 2: Использовать общий скрипт deploy_complete.sh

**На сервере выполните:**

```bash
cd /root/rbna-temp

# Проверьте, есть ли deploy_complete.sh
ls -la deploy*.sh

# Если есть, используйте его:
chmod +x deploy_complete.sh
./deploy_complete.sh 151.245.137.147 https://github.com/shirokovmv1/RBNA-portal.git
```

### Вариант 3: Загрузить скрипт через SCP

**На вашем локальном компьютере:**

```bash
scp deploy_to_151.245.137.147.sh root@151.245.137.147:/root/
```

**Затем на сервере:**

```bash
chmod +x /root/deploy_to_151.245.137.147.sh
/root/deploy_to_151.245.137.147.sh
```

---

## 🚀 Быстрое решение (выполните на сервере)

**Скопируйте и выполните эту команду на сервере:**

```bash
cd /root/rbna-temp && ls -la deploy*.sh
```

Если увидите `deploy_complete.sh`, используйте его:

```bash
chmod +x deploy_complete.sh
./deploy_complete.sh 151.245.137.147 https://github.com/shirokovmv1/RBNA-portal.git
```

---

## 📝 Или создайте скрипт вручную

Я создал файл `CREATE_DEPLOY_SCRIPT_ON_SERVER.sh` - загрузите его на сервер и выполните, он создаст нужный скрипт.

**Или выполните команды из `VPS_DEPLOYMENT.md` пошагово.**

---

**Самое простое: используйте `deploy_complete.sh` если он есть в репозитории!**
