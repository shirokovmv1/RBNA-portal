# Руководство по развертыванию на VPS - Навигация

## 📚 Документация для развертывания

### Основные документы:

1. **`VPS_DEPLOYMENT.md`** ⭐ - **НАЧНИТЕ ОТСЮДА**
   - Полное пошаговое руководство
   - Детальные инструкции для каждого шага
   - Примеры конфигураций
   - Решение проблем

2. **`VPS_DEPLOYMENT_QUICK.md`** - Быстрые команды
   - Все команды в одном месте
   - Для опытных пользователей
   - Быстрая справка

3. **`VPS_CHECKLIST.md`** - Чеклист
   - Список всех шагов
   - Для отслеживания прогресса
   - Гарантия ничего не пропустить

4. **`deploy.sh`** - Скрипт автоматизации
   - Автоматическая установка зависимостей
   - Настройка базовой инфраструктуры
   - Экономия времени

---

## 🎯 Быстрый старт

### Вариант 1: Автоматический (рекомендуется для начала)

```bash
# На вашем локальном компьютере
# 1. Загрузите deploy.sh на сервер
scp deploy.sh root@your-server-ip:/root/

# 2. Подключитесь к серверу
ssh root@your-server-ip

# 3. Запустите скрипт
chmod +x deploy.sh
./deploy.sh yourdomain.com

# 4. Следуйте инструкциям на экране
```

### Вариант 2: Ручной (для полного контроля)

1. Откройте `VPS_DEPLOYMENT.md`
2. Следуйте инструкциям шаг за шагом
3. Используйте `VPS_CHECKLIST.md` для отслеживания прогресса

---

## 📋 Что нужно перед началом

- [ ] VPS сервер с Ubuntu 20.04+ или Debian 11+
- [ ] Root доступ или пользователь с sudo
- [ ] Доменное имя (или IP адрес)
- [ ] SSH доступ к серверу
- [ ] Проект готов к развертыванию

---

## 🔄 Процесс развертывания (кратко)

1. **Подготовка сервера** (5 минут)
   - Обновление системы
   - Установка базовых пакетов

2. **Установка зависимостей** (10 минут)
   - Python, PostgreSQL, Nginx, Node.js

3. **Настройка базы данных** (5 минут)
   - Создание БД и пользователя

4. **Развертывание Backend** (15 минут)
   - Загрузка кода
   - Настройка виртуального окружения
   - Миграции и статика

5. **Настройка Gunicorn** (5 минут)
   - Конфигурация
   - Systemd сервис

6. **Развертывание Frontend** (10 минут)
   - Сборка production версии

7. **Настройка Nginx** (10 минут)
   - Конфигурация reverse proxy

8. **SSL сертификат** (5 минут)
   - Let's Encrypt

9. **Файрвол** (2 минуты)
   - Настройка UFW

**Общее время: ~1 час**

---

## 🆘 Если что-то пошло не так

### Проблемы с подключением
- Проверьте SSH ключи
- Убедитесь, что порт 22 открыт

### Проблемы с правами
- Используйте `sudo` для команд требующих прав
- Проверьте владельца файлов: `ls -la`

### Проблемы с сервисами
- Проверьте логи: `sudo journalctl -u service-name -f`
- Проверьте статус: `sudo systemctl status service-name`

### Проблемы с базой данных
- Проверьте подключение: `sudo -u postgres psql -d rbna_portal`
- Проверьте .env файл

---

## 📞 Полезные команды

```bash
# Статус всех сервисов
sudo systemctl status rbna-portal nginx postgresql

# Логи приложения
sudo journalctl -u rbna-portal -f

# Перезапуск сервисов
sudo systemctl restart rbna-portal
sudo systemctl reload nginx

# Проверка конфигурации Nginx
sudo nginx -t

# Проверка портов
sudo netstat -tlnp | grep -E '8000|80|443'
```

---

## ✅ После развертывания

- [ ] Приложение доступно по HTTPS
- [ ] API работает
- [ ] Frontend загружается
- [ ] Админка Django доступна
- [ ] Все сервисы запускаются автоматически
- [ ] SSL сертификат обновляется автоматически
- [ ] Логи настроены

---

## 📚 Дополнительные ресурсы

- [Django Deployment](https://docs.djangoproject.com/en/4.2/howto/deployment/)
- [Gunicorn Docs](https://docs.gunicorn.org/)
- [Nginx Docs](https://nginx.org/en/docs/)
- [Let's Encrypt](https://letsencrypt.org/docs/)

---

**Начните с `VPS_DEPLOYMENT.md` для детальных инструкций! 🚀**
