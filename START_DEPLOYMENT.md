# 🚀 Начнем развертывание!

## Мне нужна информация:

**Ответьте на эти вопросы, и я подготовлю всё для автоматического развертывания:**

1. **IP адрес вашего VPS сервера:** `________________`
2. **Доменное имя** (если есть, или оставьте пустым): `________________`
3. **Проект в Git репозитории?** (да/нет)
4. **Если да, URL репозитория:** `________________`

---

## Или выполните команды сами:

Я создал полностью автоматический скрипт. Выполните:

### Вариант A: Если проект в Git

```bash
# 1. Загрузите скрипт на сервер
scp deploy_complete.sh root@YOUR-SERVER-IP:/root/

# 2. Подключитесь к серверу
ssh root@YOUR-SERVER-IP

# 3. Запустите развертывание
chmod +x /root/deploy_complete.sh
./deploy_complete.sh YOUR-DOMAIN.com https://github.com/shirokovmv1/RBNA-portal.git
```

### Вариант B: Если проект не в Git

```bash
# 1. Загрузите скрипты
scp deploy_full.sh deploy_application.sh root@YOUR-SERVER-IP:/root/

# 2. Подготовка сервера
ssh root@YOUR-SERVER-IP
chmod +x deploy_full.sh deploy_application.sh
./deploy_full.sh YOUR-DOMAIN.com

# 3. Загрузите проект
scp -r backend/ frontend/ rbna@YOUR-SERVER-IP:/home/rbna/rbna-portal/

# 4. Развертывание
ssh rbna@YOUR-SERVER-IP
cp /root/deploy_application.sh /home/rbna/rbna-portal/
cd /home/rbna/rbna-portal
chmod +x deploy_application.sh
./deploy_application.sh YOUR-DOMAIN.com
```

---

## 🔐 Если хотите, чтобы я выполнил команды

Мне нужен доступ к серверу. Безопасные варианты:

1. **Создайте временного пользователя с sudo правами**
2. **Дайте мне IP и имя пользователя**
3. **Я выполню команды через SSH**

Или просто дайте мне IP адрес, и я подготовлю все команды специально для вашего сервера!

---

**Дайте мне IP адрес сервера, и я начну развертывание! 🚀**
