# Аудит безопасности RBNA Portal

## Дата проверки: 05.02.2026

## 🔴 Критические уязвимости

### 1. Отсутствие аутентификации и авторизации
**Уровень риска**: 🔴 КРИТИЧЕСКИЙ

**Проблема**:
- API endpoints не защищены аутентификацией
- Любой пользователь может выполнять CRUD операции без проверки прав
- Нет проверки ролей на уровне API

**Расположение**:
- `backend/api/views.py` - все ViewSets не имеют `permission_classes`
- `backend/api/urls.py` - нет защиты маршрутов

**Рекомендации**:
```python
# Добавить в views.py
from rest_framework.permissions import IsAuthenticated, IsAdminUser, DjangoModelPermissions

class ProjectViewSet(viewsets.ModelViewSet):
    permission_classes = [IsAuthenticated]
    # или для более детального контроля:
    # permission_classes = [DjangoModelPermissions]
```

### 2. Секретный ключ в коде
**Уровень риска**: 🔴 КРИТИЧЕСКИЙ

**Проблема**:
```python
SECRET_KEY = 'django-insecure-dev-key-change-in-production'
```

**Риски**:
- Секретный ключ захардкожен в коде
- Попадет в систему контроля версий
- Компрометация сессий и токенов

**Рекомендации**:
```python
# Использовать переменные окружения
import os
SECRET_KEY = os.environ.get('SECRET_KEY')
if not SECRET_KEY:
    raise ValueError("SECRET_KEY environment variable is required")
```

### 3. DEBUG режим включен
**Уровень риска**: 🔴 КРИТИЧЕСКИЙ

**Проблема**:
```python
DEBUG = True
```

**Риски**:
- Раскрытие детальной информации об ошибках
- Показ стека вызовов и путей к файлам
- Утечка конфиденциальной информации

**Рекомендации**:
```python
DEBUG = os.environ.get('DEBUG', 'False') == 'True'
```

### 4. ALLOWED_HOSTS = ['*']
**Уровень риска**: 🟡 ВЫСОКИЙ

**Проблема**:
```python
ALLOWED_HOSTS = ['*']
```

**Риски**:
- Уязвимость к Host header атакам
- Возможность подмены домена

**Рекомендации**:
```python
ALLOWED_HOSTS = os.environ.get('ALLOWED_HOSTS', 'localhost,127.0.0.1').split(',')
```

## 🟡 Высокие риски

### 5. Отсутствие валидации входных данных
**Уровень риска**: 🟡 ВЫСОКИЙ

**Проблема**:
- Нет явной валидации в сериализаторах для критичных полей
- Фильтры принимают любые значения без проверки

**Примеры**:
```python
# В views.py фильтры принимают любые значения
filterset_fields = ['project', 'contractor', 'status']
# Нет проверки, что project/contractor существуют
```

**Рекомендации**:
```python
# Добавить кастомные валидаторы
class ContractSerializer(serializers.ModelSerializer):
    def validate_project(self, value):
        if not Project.objects.filter(id=value.id).exists():
            raise serializers.ValidationError("Project does not exist")
        return value
```

### 6. Отсутствие rate limiting
**Уровень риска**: 🟡 ВЫСОКИЙ

**Проблема**:
- Нет ограничения частоты запросов
- Возможность DDoS атак
- Нет защиты от брутфорса

**Рекомендации**:
```python
# Установить django-ratelimit
INSTALLED_APPS = [
    'ratelimit',
]

# В views.py
from django_ratelimit.decorators import ratelimit

@ratelimit(key='ip', rate='100/h', method='GET')
def list(self, request):
    ...
```

### 7. CORS настроен слишком широко
**Уровень риска**: 🟡 ВЫСОКИЙ

**Проблема**:
```python
CORS_ALLOW_CREDENTIALS = True
# Разрешены только localhost, но для production нужно ужесточить
```

**Рекомендации**:
```python
# Для production
CORS_ALLOWED_ORIGINS = os.environ.get('CORS_ALLOWED_ORIGINS', '').split(',')
CORS_ALLOW_CREDENTIALS = True
CORS_ALLOW_METHODS = ['GET', 'POST', 'PUT', 'PATCH', 'DELETE', 'OPTIONS']
CORS_ALLOW_HEADERS = ['Content-Type', 'Authorization']
```

### 8. Отсутствие HTTPS
**Уровень риска**: 🟡 ВЫСОКИЙ

**Проблема**:
- Нет принудительного HTTPS
- Данные передаются в открытом виде

**Рекомендации**:
```python
# В settings.py
SECURE_SSL_REDIRECT = True
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True
```

## 🟢 Средние риски

### 9. Хранение данных в localStorage
**Уровень риска**: 🟢 СРЕДНИЙ

**Проблема**:
```typescript
localStorage.setItem('currentUserId', user.id.toString());
```

**Риски**:
- XSS атаки могут получить доступ к localStorage
- Данные не шифруются

**Рекомендации**:
- Использовать httpOnly cookies для токенов
- Для прототипа приемлемо, но для production нужна настоящая аутентификация

### 10. Отсутствие CSRF защиты для API
**Уровень риска**: 🟢 СРЕДНИЙ

**Проблема**:
- DRF по умолчанию не требует CSRF токены для API
- Но нужно явно настроить для безопасности

**Рекомендации**:
```python
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': [
        'rest_framework.authentication.SessionAuthentication',
        'rest_framework.authentication.TokenAuthentication',
    ],
}
```

### 11. SQLite в production
**Уровень риска**: 🟢 СРЕДНИЙ

**Проблема**:
- SQLite не подходит для production
- Нет поддержки конкурентных записей
- Нет масштабируемости

**Рекомендации**:
```python
# Использовать PostgreSQL для production
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': os.environ.get('DB_NAME'),
        'USER': os.environ.get('DB_USER'),
        'PASSWORD': os.environ.get('DB_PASSWORD'),
        'HOST': os.environ.get('DB_HOST', 'localhost'),
        'PORT': os.environ.get('DB_PORT', '5432'),
    }
}
```

### 12. Отсутствие логирования безопасности
**Уровень риска**: 🟢 СРЕДНИЙ

**Проблема**:
- Нет логирования попыток доступа
- Нет аудита действий пользователей

**Рекомендации**:
```python
import logging

logger = logging.getLogger('security')

class ProjectViewSet(viewsets.ModelViewSet):
    def create(self, request, *args, **kwargs):
        logger.info(f"User {request.user.id} created project")
        return super().create(request, *args, **kwargs)
```

## ✅ Положительные моменты

1. ✅ Использование Django ORM (защита от SQL инъекций)
2. ✅ Валидаторы в моделях (MinValueValidator, MaxValueValidator)
3. ✅ CSRF middleware включен
4. ✅ Security middleware включен
5. ✅ Нет использования eval() или опасных функций
6. ✅ Нет innerHTML/dangerouslySetInnerHTML во frontend
7. ✅ Использование TypeScript для типобезопасности
8. ✅ Валидация паролей настроена

## 📋 План действий по приоритетам

### Немедленно (критично):
1. ✅ Вынести SECRET_KEY в переменные окружения
2. ✅ Отключить DEBUG для production
3. ✅ Настроить ALLOWED_HOSTS правильно
4. ✅ Добавить аутентификацию и авторизацию

### В ближайшее время (высокий приоритет):
5. ✅ Добавить валидацию входных данных
6. ✅ Настроить rate limiting
7. ✅ Ужесточить CORS настройки
8. ✅ Включить HTTPS

### Для production (средний приоритет):
9. ✅ Перейти на PostgreSQL
10. ✅ Добавить логирование безопасности
11. ✅ Настроить мониторинг и алерты
12. ✅ Реализовать настоящую аутентификацию (JWT токены)

## 🔧 Рекомендуемые инструменты

1. **django-ratelimit** - для ограничения запросов
2. **djangorestframework-simplejwt** - для JWT аутентификации
3. **django-cors-headers** - уже используется, нужно настроить
4. **django-environ** - для управления переменными окружения
5. **django-auditlog** - для аудита действий
6. **sentry** - для мониторинга ошибок

## 📝 Заключение

Код написан с использованием безопасных практик Django и React, но для production требуется:
- Настройка аутентификации и авторизации
- Вынос секретов в переменные окружения
- Отключение DEBUG режима
- Настройка HTTPS и CORS
- Переход на production-ready базу данных

**Текущий статус**: ✅ Подходит для прототипа, ⚠️ требует доработки для production
