# Инструкция по автоматическому развертыванию

## 🎯 Самый простой способ

### Вариант 1: Полностью автоматический (если проект в Git)

```bash
# 1. Загрузите скрипт на сервер
scp deploy_complete.sh root@your-server-ip:/root/

# 2. Подключитесь к серверу
ssh root@your-server-ip

# 3. Запустите один скрипт - всё сделается автоматически!
chmod +x deploy_complete.sh
./deploy_complete.sh yourdomain.com https://github.com/your-username/RBNA-portal.git
```

**Готово!** Через 20-30 минут приложение будет развернуто.

---

### Вариант 2: Двухэтапный (если проект не в Git)

#### Этап 1: Подготовка сервера

```bash
# Загрузите скрипты
scp deploy_full.sh deploy_application.sh root@your-server-ip:/root/

# Подключитесь к серверу
ssh root@your-server-ip

# Запустите подготовку
chmod +x deploy_full.sh deploy_application.sh
./deploy_full.sh yourdomain.com
```

#### Этап 2: Загрузка проекта

**На вашем локальном компьютере:**

```bash
scp -r backend/ frontend/ rbna@your-server-ip:/home/rbna/rbna-portal/
```

#### Этап 3: Развертывание

**На сервере:**

```bash
# Подключитесь как пользователь rbna
su - rbna

# Скопируйте скрипт
cp /root/deploy_application.sh /home/rbna/rbna-portal/
cd /home/rbna/rbna-portal
chmod +x deploy_application.sh

# Запустите развертывание
./deploy_application.sh yourdomain.com
```

**Готово!**

---

## 📋 Что делают скрипты

### `deploy_full.sh` - Подготовка сервера
- ✅ Обновляет систему
- ✅ Устанавливает Python, PostgreSQL, Nginx, Node.js
- ✅ Создает пользователя rbna
- ✅ Настраивает PostgreSQL и создает БД
- ✅ Настраивает файрвол
- ✅ Создает структуру директорий

### `deploy_application.sh` - Развертывание приложения
- ✅ Настраивает виртуальное окружение
- ✅ Устанавливает зависимости
- ✅ Создает .env с правильными настройками
- ✅ Настраивает PostgreSQL
- ✅ Применяет миграции
- ✅ Собирает статические файлы
- ✅ Настраивает Gunicorn и systemd
- ✅ Собирает frontend
- ✅ Настраивает Nginx
- ✅ Устанавливает SSL

### `deploy_complete.sh` - Всё в одном
- ✅ Делает всё вышеперечисленное автоматически
- ✅ Работает если проект в Git репозитории

---

## ⚡ Быстрая команда (копировать-вставить)

```bash
# На вашем локальном компьютере
DOMAIN="yourdomain.com"
GIT_REPO="https://github.com/your-username/RBNA-portal.git"

# Загрузить скрипт
scp deploy_complete.sh root@your-server-ip:/root/

# Подключиться и запустить
ssh root@your-server-ip "chmod +x /root/deploy_complete.sh && /root/deploy_complete.sh $DOMAIN $GIT_REPO"
```

---

## ✅ После развертывания

### 1. Создайте суперпользователя Django

```bash
ssh rbna@your-server-ip
cd /home/rbna/rbna-portal/backend
source venv/bin/activate
python manage.py createsuperuser
```

### 2. Загрузите демо-данные (опционально)

```bash
python manage.py seed_data
```

### 3. Проверьте работу

Откройте в браузере:
- `https://yourdomain.com` - Frontend
- `https://yourdomain.com/api/users/` - API
- `https://yourdomain.com/admin/` - Админка Django

---

## 🔍 Проверка статуса

```bash
# Все сервисы работают?
sudo systemctl status rbna-portal nginx postgresql

# Логи приложения
sudo journalctl -u rbna-portal -f

# Проверка API
curl https://yourdomain.com/api/users/
```

---

## 🆘 Если что-то пошло не так

### Скрипт остановился с ошибкой

1. Проверьте логи:
   ```bash
   sudo journalctl -u rbna-portal -n 50
   ```

2. Проверьте вручную:
   ```bash
   cd /home/rbna/rbna-portal/backend
   source venv/bin/activate
   python manage.py check
   ```

### SSL не установился

```bash
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com
```

### Проект не загрузился

Проверьте права и наличие файлов:
```bash
ls -la /home/rbna/rbna-portal/backend/
ls -la /home/rbna/rbna-portal/frontend/
```

---

## 📚 Дополнительная информация

- Полное руководство: `VPS_DEPLOYMENT.md`
- Быстрые команды: `VPS_DEPLOYMENT_QUICK.md`
- Чеклист: `VPS_CHECKLIST.md`

---

**Время развертывания: ~20-30 минут полностью автоматически! 🚀**
