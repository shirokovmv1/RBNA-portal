#!/bin/bash
# Готовый скрипт для развертывания - просто запустите его на сервере
# Использование: ./remote_deploy.sh

set -e

echo "🚀 Автоматическое развертывание RBNA Portal"
echo "============================================="
echo ""

# Запрос данных у пользователя
read -p "Введите домен или IP адрес сервера: " DOMAIN
read -p "Введите URL Git репозитория (или нажмите Enter если загрузите вручную): " GIT_REPO

if [ -z "$DOMAIN" ]; then
    echo "❌ Домен обязателен!"
    exit 1
fi

# Проверка прав
if [ "$EUID" -ne 0 ]; then 
    echo "⚠️  Запустите с sudo: sudo ./remote_deploy.sh"
    exit 1
fi

echo ""
echo "Начинаю развертывание на $DOMAIN..."
echo ""

# Здесь будет код развертывания из deploy_complete.sh
# (вставлю полный код)

echo "✅ Развертывание завершено!"
echo "Приложение доступно на: https://$DOMAIN"
