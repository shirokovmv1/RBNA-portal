# Чек‑лист администратора портала

## Перед запуском
- Настроить `apiUrl` в `app.js`
- Проверить доступность `https://<host>/bso_portal/api/news.php`
- Создать `api/data/users.json` из шаблона `docs/users.json.example`
- Установить свой пароль (см. `docs/password-hash.php`)

## После запуска
- В админке отображается: **🌐 Серверное (Synology)**
- Работают CRUD операции
- Импорт/экспорт CSV работает с кириллицей
- В `api/data/*.json` фиксируются изменения

## Обновления
- Перед обновлением сохранить `api/data/`
- После обновления проверить вход в админку и CRUD

## Резервное копирование
- Периодический бэкап `api/data/`
