# Аудит безопасности: Анализ утечек данных

**Дата:** 2026-02-03  
**Версия портала:** текущая  
**Статус:** Критический

---

## 🔴 КРИТИЧЕСКИЕ УЯЗВИМОСТИ

### 1. Генерация PDF через внешний CDN (PDFMake)

**Местоположение:**
- `it-help.html` (строки 200-201)
- `forms/contract-request.html` (строки 8-9)
- `app.js` (строки 3156-3208, 3227-3274)

**Проблема:**
- Библиотека `pdfmake` загружается с внешнего CDN (`cdnjs.cloudflare.com`)
- PDF генерируется **на клиенте** (в браузере пользователя)
- В PDF попадают **персональные данные**:
  - ФИО сотрудника
  - Email сотрудника
  - Телефон для связи
  - Описание проблемы (может содержать конфиденциальную информацию)
  - Номер заявки
  - Дата и время заявки

**Риски:**
1. **Утечка данных через CDN**: Внешний CDN может логировать запросы, отслеживать использование
2. **Утечка данных через браузер**: PDF генерируется в браузере, данные проходят через JavaScript движок
3. **Утечка через расширения браузера**: Расширения могут перехватывать данные во время генерации PDF
4. **Утечка через DevTools**: Пользователь может видеть все данные в консоли разработчика
5. **PDF отправляется на сервер в base64**: Данные передаются в открытом виде (хотя и через HTTPS)

**Рекомендации:**
- ✅ **ВАРИАНТ 1 (Предпочтительный)**: Генерировать PDF на сервере (PHP библиотека, например `tcpdf` или `fpdf`)
- ✅ **ВАРИАНТ 2**: Загружать `pdfmake` локально (не с CDN), минимизировать передачу данных в браузер
- ✅ **ВАРИАНТ 3**: Использовать Content Security Policy (CSP) для ограничения внешних скриптов

---

### 2. Отправка персональных данных через Email

**Местоположение:**
- `api/helpdesk.php` (строки 72-96, 177-208)

**Проблема:**
- Все данные формы helpdesk отправляются через PHP `mail()` на внешние email адреса:
  - `helpdesk@bso-cc.ru` (получает все данные + вложения)
  - Email сотрудника (получает PDF с данными)
- Данные передаются через SMTP сервер (может быть внешним)
- Вложения (файлы пользователя) отправляются без дополнительной проверки содержимого

**Риски:**
1. **Утечка через SMTP**: Данные проходят через почтовый сервер, могут логироваться
2. **Утечка через email провайдера**: Email провайдер может иметь доступ к содержимому писем
3. **Утечка через вложения**: Файлы могут содержать конфиденциальную информацию (скриншоты, документы)
4. **Отсутствие шифрования**: Email может передаваться в открытом виде (зависит от настроек SMTP)

**Рекомендации:**
- ✅ Использовать SMTP с обязательным TLS/SSL шифрованием
- ✅ Настроить внутренний почтовый сервер (если возможно)
- ✅ Добавить предупреждение пользователю о конфиденциальности данных
- ✅ Рассмотреть возможность отправки ссылки на заявку вместо полных данных в email

---

### 3. Экспорт контактов в CSV

**Местоположение:**
- `app.js` (строки 2757-2786)

**Проблема:**
- Все контакты экспортируются в CSV файл, который скачивается **локально на компьютер пользователя**
- CSV содержит **персональные данные**:
  - ФИО
  - Должность
  - Компания
  - Внутренний номер
  - Дата рождения
  - Контактный телефон
  - Email

**Риски:**
1. **Утечка через файловую систему**: CSV файл сохраняется на компьютере пользователя, может быть доступен другим приложениям
2. **Утечка через облачные синхронизации**: Если компьютер синхронизируется с облаком (OneDrive, Google Drive), файл может попасть в облако
3. **Отсутствие контроля**: После скачивания файла нет контроля над его распространением
4. **Доступность для всех**: Любой пользователь с доступом к админ-панели может экспортировать все контакты

**Рекомендации:**
- ✅ Добавить логирование экспорта (кто, когда экспортировал)
- ✅ Ограничить доступ к экспорту (только определенные роли)
- ✅ Добавить предупреждение о конфиденциальности перед экспортом
- ✅ Рассмотреть возможность экспорта только части данных (без даты рождения, внутренних номеров)

---

## 🟡 СРЕДНИЕ УЯЗВИМОСТИ

### 4. Хранение данных в localStorage

**Местоположение:**
- `app.js` (множественные места)
- `admin.html` (строки 798-850)

**Проблема:**
- Данные хранятся в `localStorage` браузера:
  - Токены авторизации (`bso_admin_auth`)
  - Все данные портала (новости, события, контакты, заявки)
  - Учетные данные админов (локальный fallback)

**Риски:**
1. **XSS атаки**: Если есть XSS уязвимость, злоумышленник может получить доступ к localStorage
2. **Доступ через DevTools**: Любой пользователь может просмотреть данные через консоль браузера
3. **Расширения браузера**: Расширения могут иметь доступ к localStorage
4. **Синхронизация браузера**: Данные могут синхронизироваться между устройствами через браузер

**Рекомендации:**
- ✅ Использовать `sessionStorage` для временных данных вместо `localStorage`
- ✅ Не хранить токены авторизации в localStorage (использовать httpOnly cookies)
- ✅ Очищать localStorage при выходе из системы
- ✅ Добавить защиту от XSS (уже частично реализовано через `escapeHtml`)

---

### 5. Загрузка внешних ресурсов (CDN)

**Местоположение:**
- `it-help.html` (строки 200-201): PDFMake с CDN
- `styles.css` (строка 7): Google Fonts

**Проблема:**
- Внешние ресурсы загружаются с:
  - `cdnjs.cloudflare.com` (PDFMake)
  - `fonts.googleapis.com` (Google Fonts)

**Риски:**
1. **Трекинг**: Внешние сервисы могут отслеживать пользователей
2. **Подмена ресурсов**: CDN может быть скомпрометирован (хотя маловероятно для крупных провайдеров)
3. **GDPR нарушения**: Загрузка ресурсов с внешних серверов может нарушать GDPR
4. **Зависимость от внешних сервисов**: Портал не работает без интернета

**Рекомендации:**
- ✅ Загружать все ресурсы локально (self-hosted)
- ✅ Использовать Subresource Integrity (SRI) для проверки целостности скриптов
- ✅ Настроить Content Security Policy (CSP)

---

### 6. Передача данных через API без дополнительного шифрования

**Местоположение:**
- `app.js` (строки 976-1009)
- Все API endpoints

**Проблема:**
- Данные передаются через HTTPS (хорошо), но:
  - Токены авторизации передаются в заголовках (может быть видно в логах сервера)
  - PDF передается в base64 (увеличение размера, но не критично)
  - Файлы передаются через FormData (может быть видно в логах)

**Риски:**
1. **Логирование на сервере**: Данные могут логироваться на сервере
2. **Промежуточные прокси**: Данные могут проходить через прокси-серверы

**Рекомендации:**
- ✅ Настроить ротацию токенов
- ✅ Ограничить логирование персональных данных на сервере
- ✅ Использовать короткое время жизни токенов

---

## 🟢 НИЗКИЕ РИСКИ (но требуют внимания)

### 7. Отсутствие валидации файлов при импорте CSV

**Местоположение:**
- `app.js` (строки 2694-2755)

**Проблема:**
- При импорте CSV нет проверки на:
  - Максимальный размер файла
  - Максимальное количество записей
  - Валидацию данных перед импортом

**Риски:**
1. **DoS атака**: Большой CSV файл может перегрузить сервер
2. **Некорректные данные**: Импорт некорректных данных может нарушить работу портала

**Рекомендации:**
- ✅ Добавить ограничение на размер CSV файла (например, 10MB)
- ✅ Добавить ограничение на количество записей (например, 1000)
- ✅ Валидировать данные перед импортом

---

### 8. Отсутствие rate limiting на API

**Проблема:**
- Нет ограничений на количество запросов к API от одного IP/пользователя

**Риски:**
1. **DoS атака**: Злоумышленник может перегрузить сервер множественными запросами
2. **Брутфорс атака**: Можно пытаться подобрать пароли без ограничений

**Рекомендации:**
- ✅ Добавить rate limiting (например, 100 запросов в минуту на IP)
- ✅ Добавить CAPTCHA для форм авторизации после нескольких неудачных попыток

---

## 📋 ПЛАН ДЕЙСТВИЙ (Приоритет)

### Критический приоритет (выполнить немедленно):

1. **Перенести генерацию PDF на сервер** (PHP библиотека)
   - Установить `tcpdf` или `fpdf` на сервере
   - Переписать `buildHelpdeskPdfDefinition` на PHP
   - Убрать зависимость от внешнего CDN для PDFMake

2. **Настроить безопасную отправку email**
   - Проверить настройки SMTP (обязательный TLS/SSL)
   - Добавить предупреждение о конфиденциальности
   - Рассмотреть возможность отправки ссылок вместо полных данных

3. **Ограничить экспорт контактов**
   - Добавить логирование экспорта
   - Ограничить доступ к экспорту (только определенные роли)
   - Добавить предупреждение перед экспортом

### Высокий приоритет (выполнить в течение недели):

4. **Улучшить безопасность localStorage**
   - Перейти на `sessionStorage` для временных данных
   - Использовать httpOnly cookies для токенов
   - Очищать данные при выходе

5. **Локализовать внешние ресурсы**
   - Скачать PDFMake локально
   - Скачать Google Fonts локально
   - Добавить SRI для проверки целостности

### Средний приоритет (выполнить в течение месяца):

6. **Добавить валидацию CSV импорта**
   - Ограничение размера файла
   - Ограничение количества записей
   - Валидация данных

7. **Добавить rate limiting**
   - Ограничение запросов на IP
   - CAPTCHA для форм

---

## 🔒 ДОПОЛНИТЕЛЬНЫЕ РЕКОМЕНДАЦИИ

1. **Content Security Policy (CSP)**
   - Настроить CSP заголовки для ограничения внешних ресурсов
   - Заблокировать inline scripts (если возможно)

2. **Логирование и мониторинг**
   - Логировать все операции с персональными данными
   - Мониторить подозрительную активность
   - Настроить алерты при аномалиях

3. **Регулярные аудиты**
   - Проводить аудит безопасности раз в квартал
   - Обновлять зависимости
   - Проверять уязвимости в используемых библиотеках

4. **Обучение пользователей**
   - Провести обучение по безопасности данных
   - Добавить инструкции по работе с конфиденциальными данными
   - Регулярно напоминать о правилах безопасности

---

## 📊 СВОДНАЯ ТАБЛИЦА РИСКОВ

| Уязвимость | Критичность | Вероятность | Влияние | Приоритет |
|------------|-------------|-------------|---------|-----------|
| PDF через CDN | 🔴 Критическая | Высокая | Высокое | 1 |
| Email с ПД | 🔴 Критическая | Средняя | Высокое | 2 |
| CSV экспорт | 🔴 Критическая | Средняя | Высокое | 3 |
| localStorage | 🟡 Средняя | Средняя | Среднее | 4 |
| Внешние CDN | 🟡 Средняя | Низкая | Среднее | 5 |
| API без шифрования | 🟡 Средняя | Низкая | Низкое | 6 |
| CSV импорт | 🟢 Низкая | Низкая | Среднее | 7 |
| Rate limiting | 🟢 Низкая | Низкая | Среднее | 8 |

---

**Составитель:** AI Assistant  
**Дата следующего аудита:** 2026-05-03
