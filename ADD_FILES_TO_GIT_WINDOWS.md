# 🔧 Добавление файлов backend/ и frontend/ в Git (Windows)

## ⚠️ ВАЖНО: Выполняйте на ЛОКАЛЬНОЙ Windows машине!

Эти команды нужно выполнять на вашем **локальном компьютере с Windows**, где находится исходный код проекта, а НЕ на сервере Linux!

---

## Быстрая инструкция

### Вариант 1: PowerShell (рекомендуется)

1. Откройте **PowerShell** на вашем Windows компьютере
2. Выполните команды:

```powershell
# Перейти в директорию проекта
cd "c:\Users\shiro\OneDrive\Personal project\BSO\portal\bso-portal"

# Удалить блокировку git (если есть)
Remove-Item -Force .git/index.lock -ErrorAction SilentlyContinue

# Добавить файлы в git
git add backend/ frontend/

# Проверить что добавилось
git status

# Закоммитить
git commit -m "Add backend and frontend files to repository"

# Отправить в GitHub
git push origin main
```

### Вариант 2: Git Bash

1. Откройте **Git Bash** на вашем Windows компьютере
2. Выполните команды:

```bash
# Перейти в директорию проекта
cd "/c/Users/shiro/OneDrive/Personal project/BSO/portal/bso-portal"

# Удалить блокировку git (если есть)
rm -f .git/index.lock

# Добавить файлы в git
git add backend/ frontend/

# Проверить что добавилось
git status

# Закоммитить
git commit -m "Add backend and frontend files to repository"

# Отправить в GitHub
git push origin main
```

### Вариант 3: Через Cursor/VS Code

1. Откройте проект в Cursor или VS Code
2. Откройте встроенный терминал (Ctrl+`)
3. Выполните команды:

```bash
# Удалить блокировку git (если есть)
rm -f .git/index.lock

# Добавить файлы
git add backend/ frontend/

# Проверить
git status

# Закоммитить
git commit -m "Add backend and frontend files to repository"

# Отправить
git push origin main
```

---

## Проверка после выполнения

После выполнения команд проверьте, что файлы добавлены:

```bash
git ls-files backend/manage.py backend/requirements.txt frontend/package.json
```

Должны увидеть пути к файлам. Если пусто - файлы все еще не добавлены.

---

## После успешной отправки в GitHub

После того как файлы будут в GitHub, можно запускать развертывание на сервере:

```bash
# На сервере выполните:
cd /root/rbna-temp
rm -rf /home/rbna/rbna-portal
# Затем запустите скрипт развертывания заново
```
