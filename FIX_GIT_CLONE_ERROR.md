# 🔧 Исправление ошибки Git Clone

## Проблема

Ошибка: `fatal: destination path '.' already exists and is not an empty directory.`

**Причина:** Скрипт пытается клонировать репозиторий в директорию, которая уже существует и не пуста.

## ✅ Решение

### Вариант 1: Очистить директорию вручную (быстро)

**На сервере выполните:**

```bash
# Удалить существующую директорию
rm -rf /home/rbna/rbna-portal

# Или если вы в ~/rbna-temp:
cd /root
rm -rf rbna-temp

# Затем запустите скрипт заново
/root/deploy_rbna.sh
```

### Вариант 2: Использовать исправленный скрипт

Я исправил скрипты - теперь они проверяют наличие директории и очищают её перед клонированием.

**Выполните на сервере:**

```bash
# Удалите старый скрипт (если есть)
rm -f /root/deploy_rbna.sh

# Создайте исправленный скрипт
cat > /root/deploy_rbna.sh << 'DEPLOYSCRIPT'
[Содержимое из DEPLOY_NOW.txt - уже исправлено]
DEPLOYSCRIPT

chmod +x /root/deploy_rbna.sh
/root/deploy_rbna.sh
```

### Вариант 3: Клонировать вручную, затем запустить скрипт

```bash
# Очистить директорию
rm -rf /home/rbna/rbna-portal

# Клонировать проект
su - rbna <<EOF
cd /home/rbna
git clone https://github.com/shirokovmv1/RBNA-portal.git rbna-portal
EOF

# Затем продолжить с шага 9 (настройка Backend)
# Или запустить скрипт заново - он пропустит клонирование
```

---

## 🚀 Рекомендуемое решение

**Выполните на сервере:**

```bash
# 1. Очистить директорию
rm -rf /home/rbna/rbna-portal

# 2. Запустить исправленный скрипт из DEPLOY_NOW.txt
# (скопируйте команду из файла DEPLOY_NOW.txt)
```

Или просто выполните:

```bash
rm -rf /home/rbna/rbna-portal && /root/deploy_rbna.sh
```

---

**Примечание:** Исправленные скрипты теперь автоматически обрабатывают эту ситуацию - они проверяют наличие директории и очищают её, если она не является git репозиторием.
