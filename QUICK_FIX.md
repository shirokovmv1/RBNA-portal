# 🚀 Быстрое решение проблемы с отсутствующими файлами

## Проблема

Файлы `backend/manage.py`, `backend/requirements.txt`, `frontend/package.json` отсутствуют в Git репозитории, поэтому после клонирования на сервере их нет.

## ✅ Решение (3 шага)

### Шаг 1: На ЛОКАЛЬНОМ Windows компьютере

Откройте PowerShell или Git Bash в директории проекта и выполните:

```bash
cd "c:\Users\shiro\OneDrive\Personal project\BSO\portal\bso-portal"
git add backend/ frontend/
git commit -m "Add backend and frontend files"
git push origin main
```

### Шаг 2: Подождите 10-30 секунд

Дождитесь, пока изменения появятся в GitHub.

### Шаг 3: На СЕРВЕРЕ выполните:

```bash
cd /root/rbna-temp
rm -rf /home/rbna/rbna-portal
# Затем запустите скрипт развертывания заново
/root/deploy_rbna_working.sh
```

---

## ⚠️ Важно

- **Шаг 1** выполняется на **вашем Windows компьютере** (локально)
- **Шаг 3** выполняется на **сервере Linux** (151.245.137.147)

Не путайте! Команды `git add` и `git push` работают только на локальной машине, где находится исходный код.
